{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run active C++ file (.exe folder)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}\\.exe\\${fileBasenameNoExtension}.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "environment": [],
            "externalConsole": false,
            "internalConsoleOptions": "neverOpen",
            "MIMode": "gdb",
            "miDebuggerPath": "gdb",
            "preLaunchTask": "C/C++: Build active file to .exe folder"
        }
    ]
}
