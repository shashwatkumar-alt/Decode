    // int *p, q;

    // (i) p is a pointer and q is an integer.  -------------------> Correct
    // (ii) p and q both are pointers.
    // (iii) P and q both are integers.
    // (iv) Syntax is incorrect.